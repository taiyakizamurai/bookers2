    <div class="container">
    <div class="row">
    <div class="col-md-3">
    <%= flash[:hoge] %>
    <%= render 'users/info', user: @user %>
    <%= render 'books/new', book: @book_new%>
    </div>
 <div class="col-md-8 offset-md-1">
  <h1>Book detail</h1>
  <table class="table table-hover table-inverse">

    <tbody>
         <tr>
           <td><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %><br>
           <%= link_to @user.name, user_path(@user.id)%></td>
           <td><%= link_to @book.title, book_path(@book.id) %></td>
           <td><%= @book.body %></td>
           <td><% if @user.id == current_user.id %>
    <%= link_to "Edit", edit_book_path(@book.id), class: "btn btn-success "  %>
    <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-danger " %></td>
         </tr>
       <% end %>
    </tbody>
  </table>
    </div>
    </div>
